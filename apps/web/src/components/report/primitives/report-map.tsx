// Canonical report map primitive — full CompSet map with no competitor panel.
// Used inside MarketSection and inside the CompSet gallery hero block.

export { ReportMap } from "@/components/report/ui/report-map";
