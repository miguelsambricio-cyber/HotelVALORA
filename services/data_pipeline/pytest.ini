[pytest]
testpaths = tests
pythonpath = .
addopts = -v --tb=short
markers =
    slow: tests that take > 1 second
    integration: requires a live database connection
    benchmark: performance measurement (requires pip install pytest-benchmark)
